---
import SpanSkill from './SpanSkill.astro'
import ModalGalerry from '../components/ModalGalerry.astro'

const { id, nombre, descripcion, imagen, enlace, tecnologias, repositorio, color } = Astro.props
---

<div
  class="flex w-full flex-col rounded-2xl border border-gray-200 bg-gray-200/20 transition-all duration-300 md:flex-col lg:flex-row dark:border-gray-700/30 dark:bg-gray-800/30"
>
  <!-- <a href="#" class="">
    {
      (
        // imagen?.length > 0
        // ? imagen.map((img: any) => {
        // return
        // <img
        //   class="h-full rounded-lg object-cover lg:max-w-[460px] lg:min-w-[460px]"
        //   src={imagen[0]}
        //   alt={'Imagen ' + nombre}
        // />
        // })
        // : null
      )
    }
  </a> -->
  <ModalGalerry id={`galeria-modal-${id}`} imagenes={imagen} />

  <div class="space-y-4 p-8">
    <h5 class="text-xl font-bold tracking-tight">
      {nombre}
    </h5>
    <p class="line line-clamp-7 text-base font-normal text-gray-700 dark:text-gray-400">
      {descripcion}
    </p>

    <div class="flex flex-wrap gap-2">
      {
        tecnologias.map((tec: string) => {
          return <SpanSkill>{tec}</SpanSkill>
        })
      }
    </div>

    <a
      href="#"
      class="inline-flex items-center justify-center gap-x-2 rounded-2xl border-1 border-teal-200 bg-teal-500 px-2 py-2 font-medium text-white transition hover:scale-110 dark:border-blue-400 dark:bg-blue-300 dark:text-black"
    >
      Ver Proyecto
    </a>
  </div>
</div>
